import SwiftUI

struct MapView: View {
    var body: some View {
        Text("Map")
            .font(.title)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    MapView()
}

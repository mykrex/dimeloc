import SwiftUI

struct AdminView: View {
    var body: some View {
        Text("Admin")
            .font(.title)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    AdminView()
}
